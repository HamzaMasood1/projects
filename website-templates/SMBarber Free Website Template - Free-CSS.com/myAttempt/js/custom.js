/* ==============================================
       LOADER -->
    =============================================== */
$(window).load(function(){
    $('preloader').on(500).fadeout();
    $('preloader').on(600).fadeout("slow");
});